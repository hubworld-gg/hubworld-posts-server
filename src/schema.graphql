type Post @key(fields: "id") {
  id: ID!
  author: User!
  content: String! # markdown
  tags: [String!]!
  reactions: [Reaction!]!
}

extend type User @key(fields: "id") {
  id: ID! @external
  posts: [Post!]!
}

type Reaction {
  type: String!
  user: User!
}
